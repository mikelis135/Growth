<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@color/colorPrimary"
    android:paddingEnd="12dp">

    <androidx.cardview.widget.CardView
        android:id="@+id/channelMediaPhotoImg"
        android:layout_width="152dp"
        android:layout_height="228dp"
        app:cardCornerRadius="8dp">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:src="@drawable/image2" />
    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/channelMediaTitleTxt"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="4dp"
        android:layout_marginTop="10dp"
        android:letterSpacing="0.04"
        android:text="The Cure For Loneliness"
        android:textColor="@color/colorWhite"
        android:textSize="17sp"
        app:layout_constraintEnd_toEndOf="@id/channelMediaPhotoImg"
        app:layout_constraintStart_toStartOf="@id/channelMediaPhotoImg"
        app:layout_constraintTop_toBottomOf="@id/channelMediaPhotoImg" />

</androidx.constraintlayout.widget.ConstraintLayout>